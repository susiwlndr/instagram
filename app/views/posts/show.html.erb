
 <!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<style>
   .container {
     width: 600px;
     margin: 10px auto;
     border: 2px solid black;
     box-sizing: border-box;
     min-height: 440px;
   }
   img {
     margin: 0px;
     padding: 0px;
     width: 300px;
   }

   caption{
   	margin: 20px;
   }

   .uraver{
    position: relative;
    width: 300px;
    height: 201px;
   }

   p{
    margin: 0;
    margin-bottom: 10px;
   }

   span{
    margin: 0;
   }
</style>
</head>
<body>
  <div class="container">
  <div class="uraver">
<p><%= @post.user.username %></p>
<%= image_tag(@post.image.url, alt: 'Image') if @post.image? %>
<span><%= @post.caption %></span><br/>
<hr>
<% @posts.each do |p| %>
  <%= p.comment %> oleh <%= p.user.username %><br/>
<% end %>
<%= form_for [@post, @comment] do |f| %>
      <div>
        <%= f.label :comment %><br/>
        <%= f.text_area :comment, rows: 5, class: 'form-control' %>
      </div>
        <%= f.submit "Comment", class: 'btn btn-primary form-control' %>
<%end%>
</div>
</div>
</body>
</html>